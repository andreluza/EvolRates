
# easily going through the folders 
require(here)

# phylogeny and traits
require(ape)
require(geiger)
require(phylobase)
require(daee) # https://github.com/vanderleidebastiani/daee
require(phytools)
require(devtools)
require(picante)
require(vegan)

# processing
require(parallel)

# spatial analyses

library (rgdal)
library (raster)
library(rgeos)
library(dismo)
library (maptools)
library (geosphere)
library (reshape)
library (maps)
require(spdep)

# plots
require(ggplot2)
library("gridExtra")
require (ggpubr)
require(ggExtra)

# models
require(nlme)
require(lme4)
require(ade4)

